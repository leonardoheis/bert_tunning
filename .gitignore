# ---- Byte-compiled / optimized / DLL files ----
__pycache__/
*.py[cod]
*$py.class
*.so

# ---- Distribution / packaging ----
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ---- PyInstaller ----
*.manifest
*.spec

# ---- Installer logs ----
pip-log.txt
pip-delete-this-directory.txt

# ---- Unit test / coverage reports ----
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/
tests/reports/

# ---- Translations ----
*.mo
*.pot

# ---- Django/Flask stuff ----
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
instance/
.webassets-cache

# ---- SQLite dev databases ----
*.db

# ---- Scrapy ----
.scrapy

# ---- Sphinx documentation ----
docs/_build/

# ---- PyBuilder ----
.pybuilder/
target/

# ---- Jupyter Notebook ----
.ipynb_checkpoints
*/.ipynb_checkpoints/*
profile_default/
ipython_config.py

# ---- pyenv / pipenv / poetry / pdm ----
.python-version
Pipfile.lock
poetry.lock
.pdm.toml
__pypackages__/

# ---- Celery ----
celerybeat-schedule
celerybeat.pid

# ---- SageMath ----
*.sage.py

# ---- Environments ----
.env
.env.*
!.env.example
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ---- Spyder / Rope ----
.spyderproject
.spyproject
.ropeproject

# ---- mkdocs ----
/site

# ---- Type checkers / linters ----
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/

# ---- Cython debug ----
cython_debug/

# ---- Datasets ----
datasets/

# ---- Data & models (ML/DL specific) ----
data/**
!data/**/
!data/**/.gitkeep
*.csv
*.tsv
*.parquet
*.feather
*.h5
*.hdf5
*.pkl
*.pickle
*.joblib
*.npy
*.npz
*.zip
*.tar
*.tar.gz
*.7z

# ---- Trained models / checkpoints ----
checkpoints/
*.pt
*.pth
*.ckpt
*.onnx
*.gguf
*.pb
*.tflite
saved_model/

# ---- Experiment tracking ----
mlruns/
mlartifacts/
wandb/
.neptune/
lightning_logs/
runs/
tb_logs/
tensorboard/

# ---- IDEs / editors ----
.idea/
.vscode/
*.swp
*.swo
*~

# ---- OS ----
.DS_Store
Thumbs.db
desktop.ini

# Virtual environments
.venv

# Secrets
.env

# Logs
logs/
*.log

# W&B
wandb/

# Reports
reports/
*.tmp
*.bak

# OS
.DS_Store
Thumbs.db
desktop.ini

# Models — large binary weights, tracked via Git LFS or stored externally
# Keep the models/ folder structure but ignore downloaded/trained weights
models/xet
models/hub/
models/bert_tunning_model/
models/bert_tunning_model_*/

# Exception: BETO v2 is tracked via Git LFS (see .gitattributes) — best-performing
# model (macro F1 0.976), kept in the repo for reproducibility.
!models/bert_tunning_model_beto_v2/
!models/bert_tunning_model_beto_v2/**

# Exception: BETO v1 (8-class, no `otro`) is tracked via Git LFS (see .gitattributes) —
# kept alongside v2 for comparison/reproducibility.
!models/bert_tunning_model_beto/
!models/bert_tunning_model_beto/**

# ood_stats.npz was previously re-ignored here specifically so accidental git operations
# (e.g. git clean) couldn't delete it — but that made it a locally-generated artifact that
# was never actually committed, and it silently disappeared at least once as a result
# (regenerable via compute-ood-stats, but broke OOD scoring with no error until noticed).
# Now committed instead, alongside the model checkpoint it belongs to, so it can't vanish.

# Cache / data
data/
bert_tunning_predictions.csv

#Sample files
samples/

# Claude Code session state
.claude/worktrees/
